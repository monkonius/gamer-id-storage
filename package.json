{
  "name": "gamer-id-storage",
  "version": "1.0.0",
  "description": "Just to test out `localStorage` and sorting table rows. Rows are sorted alphabetically by platform.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss -i ./src/input.css -o ./docs/style.css --watch",
    "build": "tailwind -i ./src/input.css -o ./docs/style.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}
